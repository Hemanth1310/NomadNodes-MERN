version: '3.8'

services:
  db:
    image: postgres:15-alpine
    container_name: nomad-mern-db
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: nomad_nodes_mern_db
    ports:
      - "5433:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

  backend:
    build:
      context: ./backend
      target: development # Change to 'production' for live deployment
    container_name: nomad-mern-backend
    ports:
      - "3001:3001"
    volumes:
      - ./backend:/app          # Hot-reload mapping
      - /app/node_modules       # Anonymous volume for dependencies
    environment:
      DATABASE_URL: "postgresql://postgres:postgres@db:5432/movie_magic_db"
    depends_on:
      - db

  frontend:
    build:
      context: ./frontend
      target: development
      # In dev, we don't use the Nginx stage, we use Vite directly
    container_name: nomad-mern-frontend
    ports:
      - "5173:5173"
    volumes:
      - ./frontend:/app
      - /app/node_modules
    environment:
      - VITE_API_URL=http://localhost:3001
    command: npm run dev -- --host 0.0.0.0

volumes:
  pgdata: